exports.stake = {
    //质押
    onNftStake: (transHash, sender, tokenId, blockNumber) => {

    },

    //解除质押
    onNftUnStaked: (transHash, staker, tokenId, blockNumber) => {

    },

    //发放质押分红
    StakePayout: (transHash, staker, tokenId, blockNumber, stakeAmount, fromBlock, toBlock) => {
        
    },
}